/** Shape version of World and SaveFile (design 01 §6). Bump with a migration in shared/migrations. */
export const SCHEMA_VERSION = 1;

/** Snapshot cadence in turns for save files (design 01 §6, design 08 §7). */
export const SNAPSHOT_EVERY_TURNS = 26;
